---
import yesNoData from "../../../data/quizzes/yes-no.json";
import MediumCard from "../../../components/common/MediumCard.astro";
import Layout from "../../../layouts/Layout.astro";
import NavBar from "../../../components/common/NavBar.astro";
import Hero from "../../../components/common/Hero.astro"
import Footer from "../../../components/common/Footer.astro"
import { AstroSeo } from "@astrolib/seo";
import quizzesData from "../../../data/quizzes/quizzes.json";
import yesNoImg from "/src/img/heroes/YESNO.jpg";

type QuizJson = {[k: string]: {
  title: string,
  instructions: string,
  rank?: number,
  tags?: string[]
}}

const quizzes = Object.entries(yesNoData as QuizJson).map(([key, value]) => {
  return {
    key,
    href: `/quizzes/yes-no/${key}`,
    title: value.title,
    instructions: value.instructions,
    rank: value.rank || 0,
    imgPath: `/img/quizzes/yes-no/${key}.jpg`,
    tags: value.tags || []
  }
}).sort((a,b) => -a.rank + b.rank);

const heroText = quizzesData.yesNo.heroText;
---
<AstroSeo
  title={"Yes/No Quizzes | Izz Hafeez"}
  description={heroText}
  canonical={`https://izzhafeez.com/quizzes/compare`}
  openGraph={{
    title: "Yes/No Quizzes | Izz Hafeez",
    description: heroText,
    url: `https://izzhafeez.com/quizzes/compare`,
    type: "website",
    images: [
      {
        url: yesNoImg.src,
        width: 800,
        height: 600,
        alt: "Yes/No Quizzes | Izz Hafeez"
      }
    ]
  }}
/>
<Layout>
  <div slot="main">
    <NavBar/>
    <Hero label="YES NO QUIZZES" text={heroText} site="quizzes"/>
    <section class="grid grid-cols-1 lg:grid-cols-2 gap-2 relative px-2 mt-2 max-w-6xl mx-auto">
      {quizzes.map(quiz => <MediumCard
        href={quiz.href}
        title={quiz.title}
        site="quizzes"
        heroText={quiz.instructions}
        key={quiz.key}
        imgPath={quiz.imgPath}
        isEager={true}
        tags={quiz.tags}/>)}
    </section>
    <Footer color="ns" isEager={true}/>
  </div>
</Layout>