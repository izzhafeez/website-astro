import { writable } from "svelte/store";

export const toAddAll = writable(null);
export const toRemoveAll = writable(null);
export const toAdd = writable(null);
export const toAddFeature = writable(null);