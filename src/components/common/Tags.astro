---
const { tags=[], site } = Astro.props;

let tagColor = "text-gray-500 dark:text-gray-300 bg-gray-300/20";
switch (site) {
  case 'apps':
    tagColor = "text-ew-500 dark:text-ew-300 bg-ew-300/20";
    break;
  case 'portfolio':
    tagColor = "text-ne-500 dark:text-ne-300 bg-ne-300/20";
    break;
  case 'quizzes':
    tagColor = "text-ns-500 dark:text-ns-300 bg-ns-300/20";
    break;
  case 'games':
    tagColor = "text-dt-500 dark:text-dt-300 bg-dt-300/20";
    break;
}
---

<div class="flex flex-wrap gap-1 my-2 max-w-sm">{tags.map((tag: string) =>
  <span class={`${tagColor} text-xs rounded-full px-2 py-1`}>{tag}</span>)}</div>